---

- hosts: "{{ h }}"
  roles:
    - { role: aur, when: "ansible_os_family == 'Archlinux'" }
    - { role: mac_keyboard, when: "ansible_os_family == 'Archlinux' and hw == 'mac'" }
    - { role: mac_facetimehd, when: "ansible_os_family == 'Archlinux' and hw == 'mac'" }
    - awscli
    - discord
    - docker
    - gcloud
    - gemini-cli
    - git
    - go
    - k8s
    - keybindings
    - neovim
    - opentofu
    - pulumi
    - signal
    - slack
    - terminal
    - terraform-ls
    - tmux
    - zsh
