---

- name: Install gemini-cli on MacOS
  ansible.builtin.package:
    name: gemini-cli
    state: present
  register: result
  failed_when:
    - '"already installed" not in result.msg'
    - '"Package installed" not in result.msg'
    - result.msg != ""
  when: ansible_facts.os_family == 'Darwin'
